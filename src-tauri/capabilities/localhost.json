{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "localhost",
  "description": "SUPERSEDED by the runtime capability `localhost-dynamic` (see lib.rs localhost_capability), which grants these permissions to the AUTO-PICKED http://localhost:<port> origin. This static file only matches the rare fixed :1430 fallback and is otherwise inert (local:false keeps it off for dev / tauri:// builds). Safe to delete once the runtime path is x-platform-verified.",
  "local": false,
  "remote": {
    "urls": ["http://localhost:1430"]
  },
  "windows": ["main"],
  "permissions": [
    "core:default",
    "pilot:default",
    "fs:default",
    "fs:allow-read-file",
    "fs:allow-read-text-file",
    "fs:allow-write-file",
    "fs:allow-exists",
    "fs:allow-stat",
    {
      "identifier": "fs:scope",
      "allow": [{ "path": "**" }, { "path": "$HOME/.sleap-rtc/**" }]
    },
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "shell:allow-open",
    "updater:default",
    "process:default",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-set-title",
    "sleap:default"
  ]
}
